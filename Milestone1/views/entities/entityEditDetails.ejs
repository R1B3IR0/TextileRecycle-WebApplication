<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donor Edit</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
<div class="edit-entity-container">
    <h2>Edit Entity</h2>
    <form action="/entities/edit/<%=entity.id%>" method="post">
        <input type="hidden" name="_id" id="_id" value="<%= entity._id %>"<%= entity._id %>>
        <div class="form-group">
            <label for="name">Name</label>
            <input type="text" name="name" id="name" placeholder="Nome" value="<%= entity.name %>" required>
        </div>
        <div class="form-group">
            <label for="description">Descrição</label>
            <input type="text" name="description" id="description" placeholder="uma breve descrição" value="<%= entity.description %>" required>
        </div>
        <div class="form-group">
            <label for="contact">Contacto</label>
            <input type="text" name="contact" id="contact" placeholder="eg. 911111111"  value="<%= entity.contact %>"required>
        </div>
        <div class="form-group">
            <label for="email">Email</label>
            <input type="text" name="email" id="email" placeholder="eg. santacasa@gmail.com" value="<%= entity.email %>" required>
        </div>

        <div class="form-group">
            <label for="countryCode">Código do País</label>
            <input type="text" name="address.countryCode" id="countryCode" placeholder="Código do País" value="<%= entity.address.countryCode %>" required>
        </div>
        <div class="form-group">
            <label for="postalCode">Código Postal</label>
            <input type="text" name="address.postalCode" id="postalCode" placeholder="Código Postal" value="<%= entity.address.postalCode %>" required>
        </div>
        <div class="form-group">
            <label for="addressLocality">Localidade</label>
            <input type="text" name="address.addressLocality" id="addressLocality" placeholder="Localidade" value="<%= entity.address.addressLocality %>" required>
        </div>
        <div class="form-group">
            <label for="streetAddress">Endereço</label>
            <input type="text" name="address.streetAddress" id="streetAddress" placeholder="Endereço" value="<%= entity.address.streetAddress %>" required>
        </div>
        <button type="submit">Update</button>
    </form>
    <!-- Button to go back -->
    <a href="/entities" onclick="history.go(-1);">Go Back</a>
</div>
</body>
</html>